# Application
NODE_ENV=development
PORT=3000

# --- PERFORMANCE TUNING ---

# Main thread's libuv thread pool size. Default is 4.
# Increase for I/O-heavy tasks on the main thread.
# MAIN_UV_THREADPOOL_SIZE=4

# Number of worker threads to spin up for module execution.
# Defaults to (number of CPU cores - 1). Set to 1 or 2 for small servers.
# WORKER_POOL_SIZE=7

# The libuv thread pool size *inside* each worker. Keep this small. Default is 2.
# WORKER_UV_THREADPOOL_SIZE=2


# --- DATABASE (PostgreSQL example) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=mydatabase


# --- SECURITY ---
JWT_SECRET=a-very-strong-and-long-secret-key-for-jwt
